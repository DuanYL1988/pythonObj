#=======================================================================
# 1. 语法类似js, def为函数关键字,不用声明返回值: def methondName(param):
# 2. (*param) 可以传入任意数量的参数
# 3. 相同目录结构下,可以使用import {文件名}来导入
# 4. from {文件名} import {函数名} 可以直接使用函数,无需输入"."
# 5. 使用as定义别名 import {文件名} as module,
#    from {文件名} import {函数名} as fun,
#=======================================================================

def test(*classNm):
    print(classNm[0])

test('aa',12)
